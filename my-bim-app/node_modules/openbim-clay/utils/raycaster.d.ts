import * as THREE from "three";
export declare class Raycaster {
    private _caster;
    private _mouse;
    domElement?: HTMLCanvasElement;
    camera?: THREE.Camera;
    private _mouseEvent;
    private _trackMouse;
    get trackMouse(): boolean;
    set trackMouse(active: boolean);
    constructor();
    cast(items: THREE.Object3D[]): THREE.Intersection<THREE.Object3D<THREE.Event>>[];
    private getMousePosition;
}
